# Literature module template

`summary.md` is the per-paper summary scaffold. `scripts/new_project.py` copies this file when creating `projects/<name>/literature/`.

File naming for instances: `YYYY-firstauthor-keyword.md` (placed under `projects/<name>/literature/summaries/`).
