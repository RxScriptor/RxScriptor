{
  "ignorePatterns": [
    { "pattern": "^http://localhost" },
    { "pattern": "^https://github.com/<org>" },
    { "pattern": "^\\(내부" }
  ],
  "timeout": "10s",
  "retryOn429": true,
  "retryCount": 3,
  "fallbackRetryDelay": "30s"
}
